# Pokedex-CLI
Pokedex CLI is a command line interface for interacting with the system to retrieve information about pokemons through an api.


1. Create the dir for pokedex cli then run npm init -y.
2. Run node install express in the same folder.
3. Run using node index.js.
4. Start with help command in the pokedex cli.
